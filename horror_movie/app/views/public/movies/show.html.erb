<div class="container">
  <div class="row py-4">
    <div clsss="offset-1">
      <h1>映画詳細</h1>
    </div>
  </div>
</div>

<div class="container">
  <div class="row m-3">
    <div class="col-md-5">
      <table class="table">
         <tbody>
           <p>投稿ユーザー：<%= @movie.user.name %></p>
          <tr>
            <td>タイトル</td>
            <td><%= @movie.title %></td>
          </tr>
          <tr>
            <td>ジャンル</td>
            <td><%= @movie.genre.name %></td>
          </tr>
          <tr>
            <td>監督</td>
            <td><%= @movie.director %></td>
          </tr>
          <tr>
            <td>鑑賞方法</td>
            <td><%= @movie.watch %></td>
          </tr>
          <tr>
            <td>面白さ</td>
            <td><%= @movie.rate %></td>
          </tr>
            <td>怖さ</td>
            <td><%= @movie.rate_horror %></td>
          <tr>
            <td>鑑賞日</td>
            <td><%= @movie.year %></td>
          </tr>
          </tbody>
      </table>
        <p>コメント件数：<%= @movie.comments.count %></p>
          <div>
            <%= form_with model: [@movie, @comment], url: public_movie_comments_path(@movie), method: :post, local: true do |f| %>
            <%= f.text_area :comment, placeholder: "コメントをここに　※ネタバレ注意",size: "30x3" %>
            <tr><%= f.submit "コメント送信する", class: 'btn btn-info' %></tr>
            <% end %>
            <p class="bg-light text-dark"><storng>ユーザーの皆さまが自由にレビューを書き、いい作品と</br>
            出会える世界観を尊重しております。</br>
            コメントをする際は、相手の誹謗中傷やレビュー投稿の</br>
            妨げになるような内容を含むことがないようご配慮を</br>
            お願いいたします。</br>
            内容によっては管理者の判断で、Genre Horrorの利用を</br>
            停止させていただく場合がございます。</storng></p>
          </div>
    </div>
          <div class="col-md-7">
            <table class="table table-hover table-inverse">
              <thead>
                <p class="p-3 mb-2 bg-dark text-white"><i class="fas fa-book-dead"></i>コメント一覧<i class="fas fa-book-dead"></i></p>
                <tr>
                  <th>ユーザー名</th>
                  <th>コメント投稿日</th>
                  <th>コメント内容</th>
                  <th></th>
                </tr>
              </thead>
            <tbody>
              <% @movie.comments.each do |comment| %>
            <tr>
              <td><%= comment.user.name %></td>
              <td><%= comment.created_at.strftime('%Y/%m/%d') %></td>
              <td><%= comment.comment %></td>
              <td><% if comment.user == current_user %>
              <%= link_to "削除", public_movie_comment_path(comment.movie, comment), method: :delete, class: 'btn btn-danger' %>
              <% end %>
              </td>
            </tr>
            <% end %>
            </table>
          </tbody>
    </div>
  </div>
</div>
